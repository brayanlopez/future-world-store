export const getCollections = async () => {
  return [{ id: "121212", title: "Sapo", handle: "sapo" }];
};

export const getCollectionProducts = async (id: string) => {
  return [{ id: "121212", title: "Sapo", handle: "sapo" }];
};
